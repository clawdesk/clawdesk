# Prometheus scrape config for ClawDesk
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "clawdesk"
    static_configs:
      - targets: ["clawdesk:18789"]
    metrics_path: "/api/v1/admin/metrics"
    scheme: http
